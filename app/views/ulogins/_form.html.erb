<%= form_for(@ulogin) do |f| %>
  <% if @ulogin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@ulogin.errors.count, "error") %> prohibited this ulogin from being saved:</h2>

      <ul>
      <% @ulogin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :emali %><br>
    <%= f.text_field :emali %>
  </div>
  <div class="field">
    <%= f.label :password %><br>
    <%= f.password_field :password %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


